{
  "name": "linkdesk-plugin-theme-zones",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "per-surface 背景机制示例主题——⑬ 纸纹分区（surface.texture 平铺纹理）+ ⑭ 影像分区（background.mode:zones 连续切片），#50.28-#50.31 壳机制第一个真实消费者——纯数据插件（无 entry/无编译表面）。独立仓可自足构建：build 走 @linkdesk/plugin-sdk 的 pack 通道（目录整树打包），不产 bundle。",
  "scripts": {
    "build": "linkdesk-plugin-sdk pack",
    "validate": "linkdesk-plugin-sdk validate",
    "lint": "linkdesk-plugin-sdk lint",
    "publish": "linkdesk-plugin-sdk publish"
  },
  "devDependencies": {
    "@linkdesk/plugin-sdk": "^0.1.14"
  }
}
